/* eslint-disable import/prefer-default-export */
const BOX_SHADOW = {
    optionShadow: '0px 2px 5px rgba(0, 0, 0, 0.15)',
    login: '0 0 62px 0 rgba(0, 0, 0, 0.1)',
};

const DROP_SHADOW = {
    // TODO: this is dummy value so we will remove
    large: '0 35px 35px rgba(0, 0, 0, 0.25)',
    'ex-Large': ['0 35px 35px rgba(0, 0, 0, 0.25)', '0 45px 65px rgba(0, 0, 0, 0.15)'],
};

export { BOX_SHADOW, DROP_SHADOW };
