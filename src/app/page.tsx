import Home from '@/components/Home';
import Layout from '@/components/Layout';

const HomePage = () => {
    return (
        <Layout>
            <Home />
        </Layout>
    );
};

export default HomePage;
